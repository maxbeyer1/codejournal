# Intended for DEVELOPMENT use only, 
# ignore if installing the extension in production
# To use: copy to .env.local (or change the envFile path in .vscode/launch.json)

# Uncomment for debug mode
# DEBUG_MODE=true

# Uncomment for LLM tracing in development
# HELICONE_API_KEY=your_helicone_api_key